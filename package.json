{
  "name": "build-express",
  "version": "1.0.0",
  "description": "-   **Layer** -> Contains the path and necessary function reference to execute when a path matches the given request.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "array-flatten": "^3.0.0",
    "http": "^0.0.1-security",
    "merge-descriptors": "^2.0.0",
    "methods": "^1.1.2",
    "setprototypeof": "^1.2.0"
  }
}
